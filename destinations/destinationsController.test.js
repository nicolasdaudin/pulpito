const destinationsController = require('./destinationsController');
const assert = require('assert').strict;

describe('destinationsController test', function () {
  it('should try to test here', function () {
    assert.ok(true);
  });
});
